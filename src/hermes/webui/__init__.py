"""Local Streamlit web UI for running and reviewing backtests (ADR-0008).

``discovery`` and ``review`` are Streamlit-free (unit-testable without the ``[ui]``
extra); ``app`` is the Streamlit page and ``launch`` the ``hermes-ui`` entry point.
"""
